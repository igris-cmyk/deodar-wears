import { PageHeader } from "@/components/ui/primitives";

export default function AccountPage() {
  return (
    <PageHeader eyebrow="Account" title="Identity foundation">
      Profile, security and preferences are available. Orders, addresses, wishlist and
      returns remain hidden until their implementation phases.
    </PageHeader>
  );
}
